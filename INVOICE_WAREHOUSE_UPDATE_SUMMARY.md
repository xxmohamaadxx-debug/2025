# ملخص تحديثات الفواتير والمستودع

## ✅ ما تم إنجازه

### 1. ملف SQL للتحديثات (`update_database_invoice_items_warehouse.sql`)
تم إنشاء ملف SQL شامل يتضمن:
- ✅ جدول `invoice_items` لعناصر الفواتير
- ✅ ربط الفواتير بالمستودع تلقائياً
- ✅ Triggers لتحديث المستودع عند إضافة/حذف عناصر الفاتورة
- ✅ Function لحساب المبلغ الإجمالي تلقائياً
- ✅ جدول `backups` للنسخ الاحتياطي
- ✅ Functions للنسخ الاحتياطي والاستعادة
- ✅ دعم متعدد اللغات في قاعدة البيانات

---

## 🔧 ما هو مطلوب تنفيذه

### 1. إصلاح PDF المشوه
**المشكلة:** النص العربي يظهر مشوهاً في PDF

**الحل المقترح:**
- استخدام `autoTable` لجميع النصوص العربية
- استخدام HTML2Canvas لتحويل HTML إلى PDF
- أو استخدام مكتبة `html2pdf.js` التي تدعم العربية بشكل أفضل

### 2. إضافة قسم/زر لإضافة فاتورة مع منتجات
- تحديث `InvoiceDialog.jsx` لإضافة جدول منتجات
- إمكانية اختيار منتجات من المستودع
- إضافة/حذف/تعديل المنتجات في الفاتورة

### 3. ربط الفواتير بالمستودع
- ✅ **تم في SQL:** Triggers تحدث المستودع تلقائياً
- مطلوب: تحديث الكود في `neonService.js` لدعم `invoice_items`
- مطلوب: تحديث `InvoiceDialog` لربط المنتجات

### 4. دعم لغات متعددة (عربي/إنجليزي/تركي)
- تحديث `pdfUtils.js` لدعم اللغات
- إضافة ترجمات للغة التركية
- اختيار اللغة عند تصدير PDF

### 5. نظام النسخ الاحتياطي
- ✅ **تم في SQL:** جدول `backups` و Functions
- مطلوب: إنشاء صفحة/مكون للنسخ الاحتياطي
- مطلوب: واجهة لاستيراد/تصدير النسخة الاحتياطية

### 6. تصدير Excel
- استخدام مكتبة `xlsx` الموجودة
- دعم لغات متعددة في Excel

---

## 📋 خطوات التنفيذ المتبقية

### المرحلة 1: إصلاح PDF (عاجل)
1. تثبيت `html2pdf.js` أو استخدام `html2canvas`
2. تحديث `pdfUtils.js` لاستخدام HTML بدلاً من النص المباشر
3. اختبار النصوص العربية

### المرحلة 2: تحديث نموذج الفاتورة
1. تحديث `InvoiceDialog.jsx`:
   - إضافة جدول المنتجات
   - إمكانية اختيار من المستودع
   - حساب المبلغ تلقائياً
2. تحديث `neonService.js`:
   - إضافة `createInvoiceItem`
   - إضافة `updateInvoiceItem`
   - إضافة `deleteInvoiceItem`
   - تحديث `createInvoiceIn/Out` لدعم العناصر

### المرحلة 3: ربط المستودع
1. التأكد من عمل Triggers في قاعدة البيانات
2. اختبار إضافة فاتورة وارد (يجب أن تزيد الكمية)
3. اختبار إضافة فاتورة صادر (يجب أن تقلل الكمية)

### المرحلة 4: اللغات المتعددة
1. تحديث `translations.js` لإضافة ترجمات تركية
2. تحديث `pdfUtils.js` لدعم اختيار اللغة
3. إضافة selector للغة في واجهة التصدير

### المرحلة 5: النسخ الاحتياطي
1. إنشاء `BackupPage.jsx`
2. إضافة أزرار تصدير/استيراد
3. اختبار النسخ والاستعادة

---

## 🗂️ ملفات SQL المطلوب تشغيلها

1. ✅ `complete_database_update.sql` - التحديثات الشاملة
2. ✅ `update_database_invoice_items_warehouse.sql` - عناصر الفواتير والمستودع

---

## 📝 ملاحظات مهمة

### النص العربي في PDF
jsPDF لا يدعم العربية بشكل جيد. الحلول المقترحة:
1. **html2pdf.js** - يدعم العربية بشكل أفضل
2. **html2canvas + jsPDF** - تحويل HTML إلى صورة ثم PDF
3. **AutoTable فقط** - استخدام autoTable لجميع النصوص

### ربط المستودع
- الفواتير الواردة (in) **تزيد** من كمية المستودع
- الفواتير الصادرة (out) **تنقص** من كمية المستودع
- يتم التحقق من الكمية المتوفرة قبل الإخراج

### النسخ الاحتياطي
- يتضمن جميع بيانات المتجر (فواتير، مستودع، موظفين، إلخ)
- يمكن استعادته في نفس المتجر أو متجر آخر
- يجب التحقق من صحة البيانات قبل الاستعادة

---

## 🔗 الملفات المرتبطة

- `src/components/invoices/InvoiceDialog.jsx` - يحتاج تحديث
- `src/lib/pdfUtils.js` - يحتاج إصلاح
- `src/lib/neonService.js` - يحتاج إضافة دوال invoice_items
- `src/lib/translations.js` - يحتاج ترجمات تركية
- `update_database_invoice_items_warehouse.sql` - ✅ تم الإنشاء

---

## ⚠️ تحذيرات

1. **قبل تشغيل SQL:** عمل نسخة احتياطية من قاعدة البيانات
2. **النسخ الاحتياطي:** يجب اختباره على بيئة تجريبية أولاً
3. **المستودع:** التأكد من صحة الكميات قبل ربط الفواتير

---

## 📞 للدعم

إذا واجهت أي مشاكل:
1. تحقق من سجلات قاعدة البيانات (Database Logs)
2. تحقق من Console المتصفح للأخطاء
3. راجع ملفات SQL للتأكد من التحديثات

---

**تاريخ الإنشاء:** 2025
**آخر تحديث:** 2025

