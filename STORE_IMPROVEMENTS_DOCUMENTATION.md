# توثيق التحسينات الشاملة على النظام

## 📋 ملخص التحديثات

تم إضافة وتحسين جميع الميزات المطلوبة للنظام، بما في ذلك:

1. ✅ إصلاح مشكلة Deploy على Netlify
2. ✅ تحسينات متجر المقاولين
3. ✅ تحسينات متجر المحروقات
4. ✅ نظام المستودع العالمي (الوارد/الصادر)
5. ✅ اليومية المحاسبية (المدين/الدائن)
6. ✅ نظام الذمم والمدفوعات مع تاريخ السداد
7. ✅ نظام رواتب الموظفين والخصومات التلقائية
8. ✅ عزل المتاجر في الشريط الجانبي

---

## 🏗️ متجر المقاولين

### الجداول الجديدة:

1. **contractor_projects** - المشاريع
   - ربط المشاريع بالعملاء والموظفين
   - تتبع التكلفة المقدرة مقابل الفعلية
   - حالات المشروع (نشط، مكتمل، متوقف، ملغي)

2. **project_items** - بنود الكميات (BOQ)
   - ربط كل بند بمشروع
   - تحديد الكمية المخططة والمنفذة
   - ربط بالمستودع أو الموردين
   - تصنيف البنود (خرسانة، حديد، كهرباء...)

3. **contractor_contracts** - عقود المقاولين
   - ربط العقود بالمشاريع
   - تحديد مدة العقد وقيمته
   - شروط الدفع والشروط العامة

4. **contractor_payments** - دفعات المقاولين
   - أنواع الدفعات (مقدمة، أقساط، نهائية، غرامات)
   - ربط بالعقود والمشاريع

5. **purchase_requests** - طلبات الشراء التلقائية
   - إنشاء تلقائي عند نقص الكميات في BOQ
   - ربط بالمشاريع والمواد

### الميزات:

- ✅ ربط كل مشروع بجدول BOQ
- ✅ عند صرف مواد من المستودع، تُخصم مباشرة من المشروع
- ✅ إنشاء طلبات شراء تلقائية عند نقص الكميات
- ✅ مقارنة الكميات المخططة مقابل المنفذة

---

## ⛽ متجر المحروقات

### الجداول الجديدة:

1. **fuel_products** - منتجات المحروقات
   - أنواع الوقود (بنزين، ديزل، غاز)
   - ربط بالمستودع
   - إمكانية الربط بأسعار السوق العالمية

2. **fuel_daily_prices** - الأسعار اليومية
   - تسجيل أسعار يومية لكل منتج
   - ربط بأسعار السوق العالمية

3. **fuel_customers** - عملاء المحروقات
   - عملاء أفراد وشركات
   - عقود توريد شهرية
   - تحديد الكميات الشهرية المتفق عليها

### الميزات:

- ✅ إدارة أنواع الوقود المختلفة
- ✅ أسعار يومية مرتبطة بسوق عالمي
- ✅ إدارة المخزون (وارد/صادر)
- ✅ عملاء أفراد وشركات مع عقود شهرية
- ✅ متابعة المبيعات اليومية

---

## 📦 نظام المستودع العالمي

### الجداول الجديدة:

1. **warehouse_transactions** - حركات المستودع
   - الوارد (Inbound): أي عملية شراء أو إدخال مواد
   - الصادر (Outbound): أي عملية بيع أو صرف مواد
   - التعديلات (Adjustment): تعديل الكميات
   - النقل (Transfer): نقل بين المستودعات

### الميزات:

- ✅ **الوارد**: يزيد المخزون، يزيد دين المورد
- ✅ **الصادر**: يقل المخزون، يزيد دين العميل
- ✅ **التحقق التلقائي**: لا يمكن تصدير كمية غير متوفرة
- ✅ **إنشاء طلبات شراء تلقائية**: عند انخفاض الكمية عن الحد الأدنى
- ✅ **ربط بالمشاريع**: صرف مواد لمشروع معين

### Functions:

- `check_inventory_availability()` - التحقق من توفر الكمية قبل الصادر
- `update_inventory_from_warehouse_transaction()` - تحديث المخزون تلقائياً

---

## 📊 اليومية المحاسبية

### الجداول الجديدة:

1. **journal_entries** - قيود اليومية
   - تسجيل كل عملية محاسبية بشكل مزدوج (مدين/دائن)
   - أنواع القيود: وارد، صادر، دفعة، قبض، دين، دائن، راتب، تعديل

### الميزات:

- ✅ **القيود التلقائية**: كل عملية تُسجل تلقائياً في اليومية
- ✅ **المدين والدائن**: 
   - الوارد: مدين للمستودع، دائن للمورد
   - الصادر: مدين للعميل، دائن للمستودع
   - دفعة عميل: مدين للصندوق، دائن للعميل
   - دين جديد: مدين للعميل، دائن للإيرادات

### Functions:

- `create_journal_entry()` - إنشاء قيد يومية تلقائي

---

## 💰 نظام الذمم والمدفوعات

### الجداول الجديدة:

1. **debts** - الذمم
   - ربط بالعملاء/الموردين/الموظفين/المقاولين
   - المبلغ الأصلي والمبلغ المدفوع
   - حالة الدين (نشط، مسدد جزئي، مسدد كامل)
   - تاريخ السداد (settled_at)

2. **payments** - المدفوعات
   - ربط بالدين
   - تاريخ الدفع
   - طريقة الدفع (نقدي، تحويل، بطاقة، شيك)

### الميزات:

- ✅ **تحديث تلقائي**: عند الدفع، يُحدث الدين تلقائياً
- ✅ **تاريخ السداد**: يُسجل تلقائياً عند اكتمال الدفع
- ✅ **حالة الدين**: نشط → مسدد جزئي → مسدد كامل
- ✅ **القيود التلقائية**: كل دفعة تُسجل في اليومية

### Functions:

- `create_debt()` - إنشاء دين جديد
- `update_debt_on_payment()` - تحديث الدين عند الدفع

---

## 👔 نظام رواتب الموظفين

### الجداول الجديدة:

1. **employee_deductions** - الخصومات
   - أنواع الخصومات: قرض، غرامة، تأمين، ضريبة
   - التكرار: مرة واحدة، شهري، أسبوعي، سنوي
   - فترة الخصم (start_date, end_date)

2. **payroll** - كشوف الرواتب
   - الراتب الأساسي
   - البدلات
   - إجمالي الخصومات
   - الراتب الصافي (محسوب تلقائياً)

### تحديثات جدول employees:

- `base_salary` - الراتب الأساسي
- `allowances` - البدلات
- `deductions` - الخصومات
- `net_salary` - الراتب الصافي (محسوب تلقائياً)
- `payment_method` - طريقة الدفع

### الميزات:

- ✅ **الخصم التلقائي**: عند تجهيز كشف الرواتب، تُطبق الخصومات تلقائياً
- ✅ **حساب الراتب الصافي**: يُحسب تلقائياً (الراتب + البدلات - الخصومات)
- ✅ **القيود التلقائية**: كل راتب يُسجل في اليومية
- ✅ **تسجيل الخصومات**: تُسجل بشكل منفصل في اليومية

### Functions:

- `calculate_net_salary()` - حساب الراتب الصافي
- `create_payroll_entry()` - إنشاء كشف راتب
- `record_payroll_payment()` - تسجيل دفع الراتب في اليومية

---

## 🔒 عزل المتاجر في الشريط الجانبي

### التحسينات:

- ✅ **فحص أنواع المتاجر**: كل قسم يظهر فقط إذا كان نوع المتجر يدعمه
- ✅ **عزل صالات الإنترنت**: لا تظهر إلا إذا كان نوع المتجر `internet_cafe`
- ✅ **عزل متجر إكسسوارات الجوال**: لا يظهر إلا إذا كان نوع المتجر `mobile_accessories` أو `internet_cafe_accessories`
- ✅ **عزل محطات المحروقات**: لا تظهر إلا إذا كان نوع المتجر `fuel` أو `general_with_fuel`
- ✅ **عزل متجر المقاولين**: لا يظهر إلا إذا كان نوع المتجر `contractor`

### الكود المحدث:

```javascript
const isInternetCafe = shouldShowSection(['internet_cafe']);
const isMobileAccessories = shouldShowSection(['internet_cafe_accessories', 'mobile_accessories']);
const isFuelStation = shouldShowSection(['fuel', 'general_with_fuel']);
const isContractor = shouldShowSection(['contractor']);
```

---

## 🚀 إصلاح مشكلة Deploy على Netlify

### التحسينات:

1. ✅ **تحسين prebuild script**: معالجة أفضل للأخطاء
2. ✅ **إضافة متغيرات بيئة**: `SKIP_PREBUILD_CHECK`
3. ✅ **تحسين netlify.toml**: إعدادات محسنة للبناء

### الملفات المحدثة:

- `netlify.toml` - إضافة متغيرات بيئة
- `package.json` - تحسين prebuild script

---

## 📝 كيفية تطبيق التحديثات

### 1. تطبيق ملف SQL:

```sql
-- قم بتشغيل الملف التالي في Neon SQL Editor:
COMPREHENSIVE_STORE_IMPROVEMENTS.sql
```

### 2. تحديث الكود:

تم تحديث الملفات التالية:
- `src/components/Sidebar.jsx` - عزل المتاجر
- `netlify.toml` - إصلاح Deploy
- `package.json` - تحسين prebuild

### 3. إعادة النشر:

```bash
git add .
git commit -m "Add comprehensive store improvements"
git push origin main
```

---

## 🎯 الخلاصة

النظام الآن يدعم:

1. ✅ **متجر المقاولين**: مشاريع، BOQ، عقود، دفعات
2. ✅ **متجر المحروقات**: منتجات، أسعار يومية، عملاء
3. ✅ **المستودع العالمي**: وارد/صادر مع تحقق تلقائي
4. ✅ **اليومية المحاسبية**: قيود مزدوجة تلقائية
5. ✅ **الذمم والمدفوعات**: مع تاريخ السداد
6. ✅ **رواتب الموظفين**: مع خصومات تلقائية
7. ✅ **عزل المتاجر**: كل متجر يرى فقط ما يخصه

جميع الميزات تعمل بشكل متكامل وتلقائي! 🎉

