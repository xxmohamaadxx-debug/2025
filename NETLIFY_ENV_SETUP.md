# دليل إضافة متغيرات البيئة في Netlify

## خطوات إضافة رابط قاعدة البيانات Neon في Netlify

### الطريقة 1: عبر Netlify Dashboard (الأسهل)

#### الخطوة 1: فتح إعدادات الموقع
1. سجل الدخول إلى [Netlify Dashboard](https://app.netlify.com)
2. اختر موقعك (Site) من القائمة
3. اذهب إلى **Site settings** من القائمة الجانبية

#### الخطوة 2: فتح صفحة متغيرات البيئة
1. في القائمة الجانبية لـ Site settings، اضغط على **Environment variables**
2. ستظهر قائمة بجميع متغيرات البيئة الحالية (إن وجدت)

#### الخطوة 3: إضافة متغير جديد
1. اضغط على زر **Add a variable** أو **Add environment variable**
2. في حقل **Key** (المفتاح)، أدخل:
   ```
   VITE_NEON_DATABASE_URL
   ```
3. في حقل **Value** (القيمة)، أدخل رابط الاتصال:
   ```
   postgresql://neondb_owner:npg_TYtfnOlr2oW7@ep-holy-frog-ahulw0nk-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
   ```
   
   **ملاحظة مهمة**: إذا كان الرابط طويلاً، تأكد من نسخه كاملاً بدون قطع

4. اختر **Scope** (النطاق):
   - **All scopes**: لتطبيق المتغير على جميع البيئات (Production, Deploy previews, Branch deploys)
   - أو اختر بيئة محددة

5. اضغط على **Save** أو **Create variable**

#### الخطوة 4: إعادة النشر
1. بعد إضافة المتغير، يجب إعادة نشر الموقع لتفعيل التغييرات
2. اذهب إلى **Deploys** من القائمة الجانبية
3. اضغط على **Trigger deploy** > **Deploy site**
4. أو قم بعمل commit جديد و push إلى GitHub (إذا كان الموقع مربوطاً بـ GitHub)

---

### الطريقة 2: عبر Netlify CLI

إذا كنت تستخدم Netlify CLI محلياً:

```bash
# تثبيت Netlify CLI (إذا لم يكن مثبتاً)
npm install -g netlify-cli

# تسجيل الدخول
netlify login

# إضافة متغير البيئة
netlify env:set VITE_NEON_DATABASE_URL "postgresql://neondb_owner:npg_TYtfnOlr2oW7@ep-holy-frog-ahulw0nk-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# للتحقق من المتغيرات
netlify env:list
```

---

### الطريقة 3: عبر netlify.toml (غير موصى به لأسباب أمنية)

⚠️ **تحذير**: لا تضيف كلمات المرور أو الروابط الحساسة مباشرة في `netlify.toml` لأن الملف قد يكون مرئياً في المستودع.

---

## التحقق من إضافة المتغير

### 1. التحقق من Netlify Dashboard
- اذهب إلى **Site settings** > **Environment variables**
- تأكد من وجود `VITE_NEON_DATABASE_URL` في القائمة
- يمكنك النقر عليه لرؤية القيمة (مخفية جزئياً لأسباب أمنية)

### 2. التحقق بعد إعادة النشر
- بعد إعادة النشر، اذهب إلى **Deploys** > اختر آخر deployment
- افتح **Published deploy** أو **Deploy log**
- تحقق من أن الموقع يعمل بدون أخطاء

### 3. التحقق من Console المتصفح
- افتح الموقع المطور
- افتح Console (F12)
- يجب ألا تظهر أخطاء مثل: `VITE_NEON_DATABASE_URL غير موجود`

---

## استكشاف الأخطاء

### المشكلة: المتغير غير موجود بعد إضافة المتغير
**الحل**:
- تأكد من كتابة اسم المتغير بشكل صحيح: `VITE_NEON_DATABASE_URL` (حساس لحالة الأحرف)
- قم بإعادة نشر الموقع بعد إضافة المتغير
- تحقق من Scope المتغير (يجب أن يكون لبيئة Production)

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل**:
- تحقق من أن رابط الاتصال صحيح وكامل
- تأكد من أن قاعدة البيانات Neon نشطة في [Neon Console](https://console.neon.tech/)
- تحقق من أن الرابط يستخدم **Connection pooling** (يجب أن يحتوي على `-pooler` في الرابط)
- تحقق من CORS settings في Neon

### المشكلة: الرابط منتهي الصلاحية
**الحل**:
1. اذهب إلى [Neon Console](https://console.neon.tech/)
2. اختر مشروعك
3. اذهب إلى **Dashboard** > **Connection Details**
4. اختر **Connection pooling**
5. انسخ الرابط الجديد
6. قم بتحديث قيمة `VITE_NEON_DATABASE_URL` في Netlify

---

## ملاحظات مهمة

### 🔒 الأمان
- **لا تشارك** رابط الاتصال مع أي شخص
- **لا ترفع** رابط الاتصال في المستودع العام (GitHub public repository)
- استخدم **Environment variables** دائماً لحفظ المعلومات الحساسة

### 📝 تنسيق رابط الاتصال
يجب أن يكون رابط الاتصال بهذا الشكل:
```
postgresql://USERNAME:PASSWORD@HOST/DATABASE?sslmode=require&channel_binding=require
```

### 🔄 تحديث المتغير
إذا احتجت تحديث قيمة المتغير:
1. اذهب إلى **Site settings** > **Environment variables**
2. اضغط على المتغير `VITE_NEON_DATABASE_URL`
3. عدل القيمة
4. احفظ التغييرات
5. أعد نشر الموقع

### 🌐 البيئات المختلفة
يمكنك إضافة قيم مختلفة للمتغير حسب البيئة:
- **Production**: للنسخة النهائية
- **Deploy previews**: للنسخ التجريبية
- **Branch deploys**: للفروع المختلفة

---

## روابط مفيدة

- [Netlify Environment Variables Documentation](https://docs.netlify.com/environment-variables/overview/)
- [Neon Console](https://console.neon.tech/)
- [Neon Connection Strings Guide](https://neon.tech/docs/connect/connection-string)

---

## مثال كامل

### في Netlify Dashboard:

**Key:**
```
VITE_NEON_DATABASE_URL
```

**Value:**
```
postgresql://neondb_owner:npg_TYtfnOlr2oW7@ep-holy-frog-ahulw0nk-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

**Scope:**
```
All scopes (Production, Deploy previews, Branch deploys)
```

---

## الخطوات النهائية

بعد إضافة المتغير:
1. ✅ تأكد من حفظ المتغير في Netlify
2. ✅ أعد نشر الموقع
3. ✅ تحقق من عمل الموقع
4. ✅ اختبر تسجيل الدخول
5. ✅ تحقق من الاتصال بقاعدة البيانات

---

إذا واجهت أي مشكلة، تحقق من:
- [DEPLOY_NETLIFY.md](./DEPLOY_NETLIFY.md) - دليل النشر الكامل
- [NEON_CONNECTION_SETUP.md](./NEON_CONNECTION_SETUP.md) - دليل إعداد اتصال Neon
- Netlify Deploy Logs - للتحقق من أخطاء البناء

