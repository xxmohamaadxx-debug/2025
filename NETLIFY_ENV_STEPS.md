# 📝 خطوات إضافة VITE_NEON_DATABASE_URL في Netlify

## ⚠️ مهم جداً:
بعد إضافة المتغير، **يجب إعادة نشر الموقع** حتى يتم دمجه في البناء!

---

## 🚀 الخطوات السريعة:

### 1️⃣ الحصول على رابط الاتصال من Neon

1. اذهب إلى: https://console.neon.tech/
2. سجل الدخول واختر مشروعك
3. اضغط على **Dashboard**
4. ابحث عن **Connection Details** أو **Connection string**
5. اختر **Connection pooling** (مهم جداً - يجب أن يحتوي على `-pooler`)
6. انسخ الرابط الكامل

**مثال على الرابط:**
```
postgresql://neondb_owner:password@ep-project-name-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

### 2️⃣ إضافة المتغير في Netlify

1. **اذهب إلى Netlify Dashboard:**
   - https://app.netlify.com
   - سجل الدخول
   - اختر موقعك (Site)

2. **افتح Environment Variables:**
   - من القائمة الجانبية: اضغط **Site settings**
   - اضغط **Environment variables**

3. **أضف المتغير:**
   - اضغط **Add a variable** أو **Add environment variable**
   - في حقل **Key**، اكتب بالضبط:
     ```
     VITE_NEON_DATABASE_URL
     ```
   - في حقل **Value**، الصق رابط الاتصال من Neon
   - في **Scope**، اختر **All scopes** (أو Production على الأقل)
   - اضغط **Save**

### 3️⃣ إعادة نشر الموقع (مهم جداً!)

بعد إضافة المتغير، **يجب إعادة نشر الموقع**:

**الطريقة الأسهل:**
1. في Netlify Dashboard
2. اضغط **Deploys** من القائمة الجانبية
3. اضغط **Trigger deploy** 
4. اختر **Clear cache and deploy site**
5. اضغط **Deploy site**

**أو عبر Git:**
```bash
git commit --allow-empty -m "Trigger rebuild for env vars"
git push origin main
```

### 4️⃣ التحقق

1. انتظر اكتمال النشر (2-5 دقائق)
2. افتح الموقع
3. افتح Console (F12)
4. يجب ألا تظهر رسالة: `VITE_NEON_DATABASE_URL غير موجود`
5. جرب تسجيل الدخول

---

## ✅ قائمة التحقق:

- [ ] تم الحصول على رابط الاتصال من Neon (Connection pooling)
- [ ] الرابط يحتوي على `-pooler`
- [ ] تم إضافة `VITE_NEON_DATABASE_URL` في Netlify
- [ ] اسم المتغير صحيح (حساس لحالة الأحرف)
- [ ] Scope يشمل Production
- [ ] تم إعادة نشر الموقع بعد إضافة المتغير
- [ ] تم مسح Build Cache (Clear cache and deploy)
- [ ] الموقع يعمل بدون أخطاء

---

## 🔍 استكشاف الأخطاء:

### ❌ المتغير لا يزال غير موجود
**الحل:**
- تحقق من اسم المتغير (يجب أن يكون بالضبط: `VITE_NEON_DATABASE_URL`)
- تأكد من إعادة نشر الموقع بعد إضافة المتغير
- امسح Build Cache وأعد النشر

### ❌ رابط الاتصال غير صحيح
**الحل:**
- تأكد من استخدام **Connection pooling** (يحتوي على `-pooler`)
- تأكد من أن الرابط يبدأ بـ `postgresql://`
- انسخ الرابط مرة أخرى من Neon Console

### ❌ قاعدة البيانات لا تستجيب
**الحل:**
- تحقق من أن قاعدة البيانات نشطة في Neon Console
- تحقق من CORS settings
- تأكد من أن قاعدة البيانات ليست في وضع Sleep

---

## 📚 ملفات المساعدة:

- [NETLIFY_ENV_QUICK_FIX.md](./NETLIFY_ENV_QUICK_FIX.md) - دليل سريع
- [NETLIFY_ENV_SETUP.md](./NETLIFY_ENV_SETUP.md) - دليل تفصيلي
- [DEPLOY_NETLIFY.md](./DEPLOY_NETLIFY.md) - دليل النشر الكامل

---

**💡 نصيحة:** بعد إضافة المتغير، دائماً أعد نشر الموقع بـ **Clear cache** لضمان دمج المتغير في البناء الجديد!

